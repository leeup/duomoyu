name = "duomoyu-hotlist"
main = "worker.js"
compatibility_date = "2024-09-01"

# 默认免费方案。部署后会得到 https://duomoyu-hotlist.<你的子域>.workers.dev
# 如果想用自定义域名,把下面的 routes 取消注释并填好域名:
# routes = [
#   { pattern = "api.example.com/*", zone_name = "example.com" }
# ]

# 防止意外日志开销
[observability]
enabled = true
